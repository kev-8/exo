[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = [
  "pip install -e .",
  "cd ui && npm ci",
]

[phases.build]
cmds = ["cd ui && npm run build"]

[start]
cmd = "uvicorn api.app:app --host 0.0.0.0 --port $PORT --workers 1"
